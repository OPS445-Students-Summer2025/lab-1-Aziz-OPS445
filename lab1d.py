#!/usr/bin/env python3

# This script performs a math calculation using objects
x = 10
y = 2
z = 5

# Calculate the result
result = x + y * z

# Show the result using string + str()
print(str(x) + ' + ' + str(y) + ' * ' + str(z) + ' = ' + str(result))
