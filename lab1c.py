#!/usr/bin/env python3

# This program uses an integer object and string together
name = 'Isaac'
age = 72

# We use str(age) to convert the number into text before printing
print(name + ' is ' + str(age) + ' years old!')
